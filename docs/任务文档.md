# 任务文档（TASK LOG）

> 本文件记录：用户的所有原始提示词、我提出的所有疑问、执行中遇到的所有问题及解决方案。
> 规则：只追加，不删除历史。每条记录带时间与状态。

---

## 0. 项目基本信息

| 项 | 内容 |
|---|---|
| 项目代号 | TermLens（暂定，可改） |
| 项目根目录 | `C:\Users\xuqingfeng\Documents\Default Project` |
| 创建日期 | 2026-09-01 |
| 当前阶段 | 阶段2：文档编写与骨架搭建（需求已澄清完毕） |
| 目标平台 | 第一阶段：Windows 桌面端；后续：手机端 |

---

## 1. 原始提示词记录

### 1.1 提示词 #1（2026-09-01）

> 我有以下几点要求。我要做一个完全新的产品，具体要求如下：
>
> 1. 专业术语自动识别与交互：
> (a) 自动识别：能够自动识别屏幕上的专业术语或概念性内容，并自动将其框出来，或者让颜色变深。
> (b) 悬停显示解释：以"大模型"、"Agent"等任何领域的专业术语为例，当鼠标悬停在术语上时，颜色自动变深，并自动显示出该术语的解释。
> (c) 点击展开与追问：再次点击该术语，会展示详细的解释说明，并且支持进一步追问。
> (d) 子概念下钻：对于该术语的下一轮概念（即子概念），也具备同样的解释功能。
>
> 2. 平台定位：
> 先做电脑端，具体是 Windows 电脑端。
>
> 3. 自动解题功能：
> 能够自动去解题，类似于 typeless 然后能够让用户自定义地去改这个相关的Key还有URL什么的。最终实现的功能的话，你就参照于typeless 把这个功能做到完成以后，注意以下几点：
> 1. 记得写任务文档：遇到任何问题，以及我的任何提示词，全都写进去。
> 2. 需求先行：对于我的要求，有不明白的直接问，先把我的需求搞明白以后再去搞这个东西。
> 3. 参考与适配：你就参考这些东西就好。最终要变成电脑端、手机端都可以使用的，但是先做电脑端就可以。

### 1.2 提示词 #2（2026-09-01，回答澄清问题）

> 我想把上面三个东西全都实现。不对不对，这4个都要实现，这几个全都要实现。我觉得这些功能都挺好的。

配套选项选择：术语识别范围 = 全屏 + 内置阅读器双轨；技术栈 = Electron + React + TS；大模型 = 额外适配 Anthropic 和 Gemini 原生协议；解题隐蔽性 = 做成开关用户自选。

### 1.3 提示词 #3（2026-09-01，关键形态修正）

> 他应该是一个就是能够利用语音去转文字的，然后又能就是对于一些题目进行简单学习，然后又能给你排版的一个东 你可以把这个东西分开，我想做成两个东西。对外展示的话，直接复刻。typeless 对内的话，开发者模式能够把那些解题什么的也能使用。

**解析出的新增硬性约束：**
- C-12 typeless 的真实形态已由用户确认为：**语音转文字 + 题目简单学习 + 自动排版** 三合一工具。以此为复刻基准，覆盖此前的网站抓取结论。
- C-13 产品拆为**两个形态（双面产品）**：
  - **对外形态（Public Mode）** = 直接复刻 typeless：语音转写 + 题目学习 + 自动排版。这是默认对外展示的完整产品。
  - **对内形态（Developer Mode）** = 隐藏的开发者模式，解锁后可使用自动解题、全屏术语识别等全部进阶能力。
- C-14 两个形态需在同一套代码内共存，通过模式开关切换，对外形态下开发者能力完全不可见（UI 与菜单均不出现）。

### 1.4 提示词 #4（2026-09-01，扩展定位与新增能力）

> 类似于在typeless之外再做一个扩展。加了一个解释的那种功能。对于马克思主义某些术语的一种自动解释。
>
> 最好能对会议自动开启录音、录屏，并提供一个开关。还有就是最好能够跨设备。

**解析出的新增硬性约束：**
- C-15 产品定位精确化：**以复刻 typeless 为底座，在其之外叠加「术语自动解释」扩展层**。术语解释是 typeless 之上的增量能力，不是替代。
- C-16 术语库必须覆盖**马克思主义理论术语**（如剩余价值、生产关系、辩证唯物主义、异化劳动、historical materialism 等），且作为首批内置的重点领域词库。领域可扩展，不限于马克思主义。
- C-17 **会议自动录音 + 自动录屏**：能检测会议场景并自动开启，必须提供用户可控开关（自动/手动/关闭三态）。
- C-18 **跨设备能力**：数据（转写稿、术语解释历史、配置）需可在多设备间同步。此条与 C-11（手机端）合并推进，需在第一阶段就把同步层的接口预留好。

---

**解析出的硬性约束：**
1. C-01 全新产品，从零开始。
2. C-02 核心功能一：屏幕专业术语自动识别 + 视觉标注（描边框 / 加深颜色）。
3. C-03 悬停即解释（hover → 变色 + 浮层简释）。
4. C-04 点击展开详解 + 支持多轮追问。
5. C-05 子概念可继续下钻，能力与父术语一致（递归无限层）。
6. C-06 首发平台 = Windows 桌面端。
7. C-07 核心功能二：自动解题（参照 typeless）。
8. C-08 API Key 与 URL（Base URL）必须用户可自定义配置。
9. C-09 必须写任务文档，记录所有提示词与问题。
10. C-10 需求不明确必须先提问澄清，不得盲目开工。
11. C-11 架构需为后续手机端预留，但当前只交付电脑端。

---

## 2. 环境勘查记录

### 2.1 勘查 #1（2026-09-01）

执行命令：`node --version; python --version; dotnet --list-sdks; cargo --version; git --version`

| 工具 | 结果 |
|---|---|
| Node.js / npm | ❌ 未安装（CommandNotFoundException） |
| Python | ✅ 3.13.14 @ `C:\Users\xuqingfeng\AppData\Local\Programs\Python\Python313\python.exe` |
| pip | ✅ 26.1.2 |
| .NET SDK | ❌ 未安装 |
| Rust / cargo | ❌ 未安装 |
| Git | ✅ 2.55.0.windows.2 |

项目目录现状：仅有 `.opencode\skills\`（与本产品无关的既有 skill 文件），无任何产品代码。

**影响技术选型的结论：**
- 选 Electron/React 需先安装 Node.js（约 100MB，可行）。
- 选 Tauri 需先安装 Rust + MSVC 工具链（较重）。
- 选 Python + PySide6 可立即开工，零环境前置成本，但后续手机端复用度低。
- → 已列入待澄清问题 Q2。

---

## 3. 待澄清问题（Q）

> 状态：⏳ 待用户回答 / ✅ 已确认 / ❌ 已否决

### Q1 「typeless」具体指什么？ ✅ 已确认

**用户原话（提示词 #2，2026-09-01）：**
> 我想把上面三个东西全都实现。不对不对，这4个都要实现，这几个全都要实现。我觉得这些功能都挺好的。

**决策 D1：四个候选形态全部实现，作为四个并列功能模块。**

**补充调研：** 已抓取 `https://typeless.io` 实际内容。该站点为新加坡 Typeless Pte. Ltd.，主营 agentic AI 安全治理三款产品（ControlZero 策略治理、AgentDefenders 欺骗式安全、EchoChamber 市场情报），**与「自动解题」「语音转写」均无关**。结论：用户记忆中的 typeless 并非该站点，故不再以该站点为参照，改以「决策 D1 的四个模块」为最终功能基准。此结论已向用户说明。

### Q1-补 技术栈选择 ✅ 已确认
**决策 D2：Electron + React + TypeScript。** 需先安装 Node.js LTS。

### Q1-补 术语识别范围 ✅ 已确认
**决策 D3：全屏任意应用（OCR + 透明覆盖层）+ 内置阅读器（精准 DOM 标注）双轨并行。**

### Q1-补 大模型接入 ✅ 已确认
**决策 D4：OpenAI 兼容协议 + Anthropic 原生协议 + Gemini 原生协议，三套 Provider 适配器。** Key / Base URL / 模型名全部用户可改（硬约束 C-08）。

### Q1-补 解题隐蔽性 ✅ 已确认
**决策 D5：防录屏隐蔽做成开关，用户自选，默认关闭（正常可见）。**

---

### 【历史】原始 Q1 选项（存档）
歧义点：市面上名字相近的产品有多个，功能差异极大：
- A. **typeless.io** —— AI 语音口述转写工具（说话 → 自动成文）。
- B. **Cluely / Interview Coder 类** —— 隐形悬浮窗，快捷键截屏 → AI 自动解答屏幕上的题目（面试/考试场景），对录屏隐藏。
- C. **TypingMind 类** —— 自带 API Key + Base URL 的第三方 AI 聊天客户端。
从「自动解题」+「自定义 Key 和 URL」推断最接近 B（截屏解题）或 C（自带 Key 的客户端）。必须由用户确认，否则整个功能二会做错方向。

### Q2 技术栈选择？ ✅ 用户选 A（见 D2）
### Q3 术语识别的作用范围？ ✅ 用户选 D（见 D3）
### Q5 默认接入哪些大模型服务？ ✅ 用户选 B（见 D4）
### Q6 解题功能是否需要「隐蔽性」？ ✅ 用户选 C（见 D5）

---

## 3.5 我自行裁定的次要决策（未打扰用户，按最优实践直接定）

> 依据：这些属于实现细节层面的等价取舍，不改变产品形态，故直接裁定并在此备案。用户如需调整可随时提出。

| 编号 | 事项 | 裁定 | 理由 |
|---|---|---|---|
| D6 | 术语判定与解释来源 | 三级瀑布：本地术语库精确命中 → SQLite 缓存命中 → 大模型实时生成并回写缓存 | 首屏响应快、Token 成本随使用递减、断网时词库仍可用 |
| D7 | 账号与云同步 | 第一阶段纯本地，不做账号；数据层抽象为 Repository 接口，后续可无痛切云端 | 最快交付电脑端（C-06），同时不堵死手机端同步（C-11） |
| D8 | 产品代号 | TermLens | 语义贴合「术语透镜」，可随时改 |
| D9 | OCR 引擎 | 主用 Windows 原生 OCR（Windows.Media.Ocr，免费、离线、中英文均支持）；降级方案 Tesseract.js | 无需联网、无额外费用、精度足够 |
| D10 | 本地存储 | better-sqlite3（缓存与历史）+ safeStorage 加密（API Key） | Key 明文落盘属安全缺陷，必须加密 |
| D11 | 追问上下文策略 | 每个术语一条独立会话线程，子概念下钻时继承父级路径作为上下文 | 保证 C-05 递归下钻时解释不跑偏 |
| D12 | 包管理器 | npm（Node 自带，零额外安装） | 环境最简 |

---

## 4. 问题与异常记录（ISSUE）

### ISSUE-001（2026-09-01）状态：已处理
**问题：** 项目目录为空，无任何既有代码可参考；`.opencode\skills\` 下存在与本产品无关的既有文件。
**处理：** 确认为全新项目（C-01），不复用 skills 目录内容，新建 `docs/` 与后续 `src/` 结构。

### ISSUE-002（2026-09-01）状态：已处理
**问题：** 未安装 Node.js / Rust / .NET SDK，无法直接开工 Electron / Tauri / WPF 方案。
**处理：** 记入环境勘查结论，转为待澄清问题 Q2 由用户定选型；若选 Electron 则先执行 Node.js 安装步骤。

### ISSUE-003（2026-09-01）状态：⏳ 阻塞中
**问题：** 「typeless」指代不明，直接影响功能二（自动解题）的产品形态。
**处理：** 已提问 Q1，等待用户确认。在确认前不动工功能二的实现。

---

## 5. 执行日志（LOG）

| 时间 | 动作 | 结果 |
|---|---|---|
| 2026-09-01 | 接收提示词 #1，解析出 11 条硬性约束 C-01~C-11 | ✅ |
| 2026-09-01 | 勘查项目目录与开发环境 | ✅ |
| 2026-09-01 | 建立本任务文档 | ✅ |
| 2026-09-01 | 提出 7 个澄清问题 Q1~Q7 | ⏳ 等待回答 |
